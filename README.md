# 🧾 Projeto: Gerador Automático de Orçamentos em PDF

Este projeto foi desenvolvido em **Python** com o objetivo de **automatizar a criação de orçamentos** a partir de dados fornecidos pelo usuário, gerando automaticamente um **arquivo PDF profissional e personalizado**.  
A biblioteca utilizada para a geração dos PDFs é a [FPDF](https://pyfpdf.github.io/fpdf2/), uma ferramenta simples e eficiente para criar documentos PDF via código.

---

## 🚀 Funcionalidades

- Geração automática de **orçamentos em PDF** a partir de informações inseridas no sistema.  
- Personalização com **cores, logotipo e layout profissional**.  
- Suporte a **cálculo automático de valores totais**.  
- Possibilidade de **alterar informações do cliente, itens e observações**.  
- Criação de arquivos prontos para impressão ou envio por e-mail.

---

## 🧠 Tecnologias Utilizadas

- **Python 3**
- **FPDF**

---

## ⚙️ Instalação

1. Clone este repositório:

   ```bash
   git clone https://github.com/joaosantosgp/projeto_pdf_automatico.git

2. Acesse o repositorio:
   
   ```bash
   cd projeto_pdf_automatico

3. Instale as dependencias (via terminal) tem que ter o Python instalado no ambiente:

   ```bash
   pip install -r requirements.txt

4. Execute:

   ```bash
   python app.py

### Preencha as informações solicitadas no terminal e tecle 'Enter' logo em seguida o PDF aparecerá no repositório em que o arquivo foi executado.